# Error: Operand types must be an integer.
# Operand must be of type Int as required by comparison operator.

type A = { m: Int }

routine foo(a: Int, m: A) -> (b: Int, m: A) {
    -> L, R (m <= 0)
    <- L, R (0 == 0)
}
