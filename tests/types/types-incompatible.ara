// Error: Structure types must have the same name for members to be compatible.
// Type Int and {a: Int,b: Int} are not compatible.

type A = { a: Int, b: Int }
type B = Int

routine foo (a: A) -> (a: B) {
}

