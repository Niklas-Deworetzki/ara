# Error: Assignment's arithmetic modifier must be an integer.
# Arithmetic expression is not of type Int as required by modification statement.

type A = {  }

routine foo (a: A) -> (b: Int) {
    b := 0 ^ (a)
}

