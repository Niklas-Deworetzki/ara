# Error: Assignment's arithmetic modifier must be an integer.
# Arithmetic expression must be of type Int as required by arithmetic assignment.

type A = {  }

routine foo (a: A) -> (b: Int) {
    b := 0 ^ (a)
}

