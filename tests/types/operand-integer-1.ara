# Error: Operand types must be an integer.
# Operand must be of type Int as required by arithmetic operator.

type A = { m: Int }

routine foo(a: Int, m: A) -> (b: Int, m: A) {
    b := a + (m + 0)
}
