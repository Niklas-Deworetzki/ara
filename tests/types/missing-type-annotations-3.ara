# Error: Without a type annotation, the type of a variable or parameter cannot be inferred.
# Type of parameter `a' cannot be inferred. Perhaps some type annotations are missing?
# Type of parameter `b' cannot be inferred. Perhaps some type annotations are missing?

routine f(a) -> (b) {
}
