// Error: Structure types must have the same name for members to be compatible.
// Structure type members #2 named `b' are not compatible.

type A = { a: Int, b: Int }
type B = { a: Int, b: A }

routine foo (a: A) -> (a: B) {
}

