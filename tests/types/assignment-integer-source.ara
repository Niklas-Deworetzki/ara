# Error: Assignment source must be integer when arithmetic modifier is present.
# Assignment source must be of type Int as required by arithmetic modifier.

type A = {  }

routine foo (a: A) -> (b: Int) {
    b := a + (3 * 2)
}

