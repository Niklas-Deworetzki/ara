# Error: Left hand side of multi-assignment has fewer resources.
# Assignment must have an equal number of resources on both sides.

routine foo(a: Int, b: Int, c: Int -> e: Int, f: Int, g: Int) {
    (a, b) := (e, f, g)
}
